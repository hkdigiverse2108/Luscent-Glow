{
  "name": "glow-haven-root",
  "version": "1.0.0",
  "description": "Root runner for Glow Haven (FastAPI + React)",
  "scripts": {
    "dev": "npx concurrently \"npm run dev --prefix Frontend\" \"cd Backend && uvicorn app.main:app --reload\"",
    "install:all": "npm install --prefix Frontend && cd Backend && pip install -r requirements.txt"
  }
}
